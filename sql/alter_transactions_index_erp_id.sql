-- Индекс для сортировки и фильтрации по ERP_ID (согласовано с рекомендуемым порядком в NocoDB).
-- У `Main` по ERP_ID уже есть уникальный ключ; для `Transactions` индекс ускоряет ORDER BY / WHERE по ERP_ID.
-- Если индекс с таким именем уже существует, пропустите выполнение или удалите дубликат вручную.
ALTER TABLE `Transactions` ADD INDEX `idx_erp_id` (`ERP_ID`);
