---
description: ERP MySQL/NocoDB — опираться на описание проекта в PROJECT_CONTEXT.md
alwaysApply: true
---

# Контекст проекта ERP

- **Корень проекта:** `/Users/vaspy/Vostok/ERP`.
- **Описание схемы, стека и договорённостей:** файл `PROJECT_CONTEXT.md` в корне репозитория.

## Когда обязательно сверяться с `PROJECT_CONTEXT.md`

Перед ответами про: таблицы `Main`, `Transactions`, логи; поля и enum; хранимые процедуры; event scheduler; интеграцию с NocoDB; пересчёт количеств и статусов — **прочитай или напомни содержимое** `PROJECT_CONTEXT.md`, если в чате ещё нет актуальной схемы.

## Поведение

- Не выдумывай имена полей и типы: они перечислены в `PROJECT_CONTEXT.md`.
- Если пользователь меняет модель данных — предложи обновить `PROJECT_CONTEXT.md` в том же PR/шаге.
- При неясности приоритета между «догадкой» и файлом — **сначала файл**, затем уточняющий вопрос.
